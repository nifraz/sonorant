# Pinned so every machine and CI runner builds with the same compiler. Bump it in its
# own commit, together with any new lints it brings.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
