[package]
name = "sonorant-dsp"
description = "Sonorant's signal processing: FFT bank, loudness and true peak, dynamic range, music features"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
rustfft = { workspace = true }
serde = { workspace = true, optional = true }

[features]
# Serialize and Deserialize for the configuration enums, for settings files.
serde = ["dep:serde"]

[dev-dependencies]
sonorant-testdata = { workspace = true }
criterion = { workspace = true }

[[bench]]
name = "hop"
harness = false

[lints]
workspace = true
